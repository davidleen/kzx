<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:gravity="left|center"
    android:orientation="horizontal"
    android:paddingBottom="6dp"
    android:paddingRight="6dp"
    android:paddingTop="6dp" >

    <TextView
        android:id="@+id/acceptanceTv"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="left|center"
        android:singleLine="true"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:textColor="#7c7c7c" />

    <ImageView
        android:id="@+id/acceptanceBg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:src="@drawable/acceptance_flag_style" />

    <!-- <FrameLayout -->
    <!-- android:id="@+id/acceptance" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="wrap_content" -->
    <!-- android:layout_gravity="center" -->
    <!-- android:layout_alignParentRight="true" -->
    <!-- android:layout_centerVertical="true" -->
    <!-- android:layout_marginLeft="6dp" -->
    <!-- android:layout_marginRight="4dp" > -->


    <!-- <ImageView -->
    <!-- android:id="@+id/acceptanceBg" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="wrap_content" -->
    <!-- android:layout_gravity="center" -->
    <!-- android:background="@drawable/imageview_copy_style" /> -->


    <!-- <CheckBox -->
    <!-- android:id="@+id/acceptanceSwitch" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="wrap_content" -->
    <!-- android:layout_gravity="center" -->
    <!-- android:background="@drawable/checkbox_copy_style" -->
    <!-- android:button="@null" -->
    <!-- android:checked="false" -->
    <!-- android:focusable="false" -->
    <!-- android:focusableInTouchMode="false" -->
    <!-- android:visibility="gone" /> -->
    <!-- </FrameLayout> -->

</LinearLayout>